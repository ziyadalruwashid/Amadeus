/**
 * Amadeus visual identity — the dark "incoming call" palette from the
 * Steins;Gate 0 app. Kept separate from the Expo template's `theme.ts`.
 */
export const AmadeusColors = {
  background: '#05060a',
  panel: '#0d1018',
  accent: '#2f9be0', // Amadeus blue
  accentDim: '#1c5f87',
  danger: '#c0392b',
  text: '#e8eef5',
  textDim: '#7d8a99',
  subtitleBg: 'rgba(0,0,0,0.55)',
} as const;
